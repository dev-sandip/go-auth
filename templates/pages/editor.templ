package pages

import "github.com/dev-sandip/go-auth/templates/layouts"

templ Editor() {
	@layouts.Base("Markdown Editor") {
		<div class="container">
			<h2>Markdown Editor</h2>
			<markdown-editor height="500px">
				# Initial content
				Write your markdown here...
			</markdown-editor>
			<button id="save-button">Save</button>
		</div>
	}
}
